services:
  kf-unified-assistant:
    build: .
    container_name: kf-unified-assistant
    restart: unless-stopped
    ports:
      - "8789:80"
    environment:
      ALLOW_REGISTRATION: "true"
      SESSION_DAYS: "30"
    volumes:
      - ./data:/var/www/data
      - ./backups:/var/www/backups
